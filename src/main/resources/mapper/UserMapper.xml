<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crud.study.infrastructure.UserMapper">


    <!-- 이메일 중복 체크 -->
    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(1)
        FROM "user"
        WHERE email = #{email}
    </select>

    <!-- 회원가입 저장 -->
    <insert id="insertUser" parameterType="com.crud.study.domain.User">
        INSERT INTO "user"(email, password, user_nm, phone_number , created_at)
        VALUES (#{email}, #{password}, #{userNm}, #{phoneNum}, now())
    </insert>

</mapper>